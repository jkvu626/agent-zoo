<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" fill="none" role="img" aria-label="AgentZoo">
  <!-- Circular emblem: concentric rings then bear face (logoex2 design) -->
  <!-- Outer thick orange ring -->
  <circle cx="50" cy="50" r="50" fill="#D9833E"/>
  <!-- Thin off-white/cream ring -->
  <circle cx="50" cy="50" r="44" fill="#F9F4ED"/>
  <!-- Thin orange ring -->
  <circle cx="50" cy="50" r="42" fill="#D9833E"/>
  <!-- Central circle background (cream) -->
  <circle cx="50" cy="50" r="40" fill="#F9F4ED"/>
  <!-- Bear face: head (warm orange) -->
  <circle cx="50" cy="50" r="18" fill="#D9833E"/>
  <!-- Ears (warm orange, rounded, extending up and slightly out) -->
  <circle cx="32" cy="34" r="10" fill="#D9833E"/>
  <circle cx="68" cy="34" r="10" fill="#D9833E"/>
  <!-- Inner ear highlights (pure white) -->
  <circle cx="32" cy="34" r="3.5" fill="#FFFFFF"/>
  <circle cx="68" cy="34" r="3.5" fill="#FFFFFF"/>
  <!-- Eyes (dark brown/black) -->
  <circle cx="44" cy="48" r="2.5" fill="#4A3728"/>
  <circle cx="56" cy="48" r="2.5" fill="#4A3728"/>
  <!-- Mouth (small upward-curving line) -->
  <path d="M 44 56 Q 50 53 56 56" stroke="#4A3728" stroke-width="1.5" fill="none" stroke-linecap="round"/>
</svg>
